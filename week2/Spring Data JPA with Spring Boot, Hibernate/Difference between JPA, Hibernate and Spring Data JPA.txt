 2. spring-data-jpa-hands-on — "Difference between JPA, Hibernate and Spring Data JPA"

| Aspect | JPA | Hibernate | Spring Data JPA |
|---|---|---|---|
| **What it is** | A specification (set of interfaces/annotations) defined by Jakarta EE | A concrete ORM framework that **implements** JPA (also has extra native features beyond JPA) | A Spring abstraction layer built **on top of** a JPA provider |
| **Role** | Defines the contract: `@Entity`, `EntityManager`, `@Id`, JPQL, etc. | Does the actual work: SQL generation, caching, dirty checking, lazy loading | Removes boilerplate: no need to write DAO/repository implementations |
| **Can it run alone?** | No — it's just interfaces, needs a provider | Yes — can be used directly without Spring | No — needs a JPA provider (usually Hibernate) underneath |
| **Example provider/tools** | N/A (spec only) | Hibernate ORM | Uses Hibernate (default) or EclipseLink under the hood |
| **Code style** | `EntityManager.persist(entity)` | Same as JPA, plus Hibernate-specific APIs (`Session`, HQL) | `repository.save(entity)` — just extend `JpaRepository` |
| **Analogy** | The "interface" / rulebook | One vendor's implementation of that rulebook | A productivity layer that hides even the JPA boilerplate |
